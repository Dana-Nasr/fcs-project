class Node:
    def __init__(self, user, weight, next):
        self.user = user                # User object
        self.weight = weight            # Weight associated with the node
        self.next ==None                # Reference to the next node in the linked list